# AI Trading Bot — Dependencies
# pip install -r requirements.txt

# Core
pandas>=2.0
numpy>=1.24

# Market data
yfinance>=0.2.30

# Testing
pytest>=7.0
pytest-asyncio>=0.21

# Existing bot dependencies (Alpaca via stdlib urllib — no extra package needed)
# Redis (optional, for live bot state persistence)
# redis>=5.0
